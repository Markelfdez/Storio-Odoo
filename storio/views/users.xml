<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="storio.list">
      <field name="name">storio list</field>
      <field name="model">storio.storio</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <record model="ir.actions.act_window" id="storio.users_action_window">
      <field name="name">Storio users</field>
      <field name="res_model">storio.user</field>
      <field name="view_mode">tree,form</field>
    </record>

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="storio.action_server">
      <field name="name">storio server</field>
      <field name="model_id" ref="model_storio_storio"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="storio" id="storio.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="storio.menu_1" parent="storio.menu_root"/>
    <menuitem name="Menu 2" id="storio.menu_2" parent="storio.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="storio.menu_1_list" parent="storio.menu_1"
              action="storio.action_window"/>
    <menuitem name="Server to list" id="storio" parent="storio.menu_2"
              action="storio.action_server"/>
    -->
  </data>
</odoo>